version: 2

models:
  - name: mview_weekly_sales
    # description: One record per registered customer. Note that not all customers have placed orders
    # columns:
    #   - name: customer_id
    #     tests:
    #       - unique
    #       - not_null
    #   - name: lifetime_value
    #     description: Total amount (in USD) the customer has spent in the store.